_target_: meds_torch.data.datamodule.MEDSDataModule.initialize

payal: chandak

dataloader:
  batch_size: 128 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
  num_workers: 0
  pin_memory: False

# train_dataset: 
#   _target_: dataset_type.pytorch_dataset
#   split: 'train'