defaults:
  - pytorch_dataset
  - _self_
dataset_cls: meds_torch.data.components.random_windows_pytorch_dataset.RandomWindowPytorchDataset

max_window_size: ${data.max_seq_len}
min_window_size: ${data.min_seq_len}
n_windows: 2
