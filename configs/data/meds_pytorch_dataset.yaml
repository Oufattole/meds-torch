_target_: meds_torch.data.meds_datamodule.MEDSDataModule
data_dir: ${paths.data_dir}
batch_size: 128 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: train,tuning,held-out
num_workers: 0
pin_memory: False
